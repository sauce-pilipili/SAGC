<form method="post" action="{{ path('app_equipes_categories_delete', {'id': equipes_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipes_category.id) }}">
    <button class="btn">Delete</button>
</form>
