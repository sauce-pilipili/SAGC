{% extends 'base.html.twig' %}

{% block title %}Infos{% endblock %}

{% block body %}
    <h1>Infos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ info.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ info.name }}</td>
            </tr>
            <tr>
                <th>Document</th>
                <td>{{ info.document }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_infos_index') }}">back to list</a>

    <a href="{{ path('app_infos_edit', {'id': info.id}) }}">edit</a>

    {{ include('infos/_delete_form.html.twig') }}
{% endblock %}
